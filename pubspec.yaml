name: bitflame
version: 0.1.0+1
publish_to: none
description: Der Pixelhain vertical slice prototype.

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.29.0
  flame_audio: ^2.11.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
    - assets/images/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  golden_toolkit: ^0.15.0
